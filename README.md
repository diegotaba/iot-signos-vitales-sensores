# This project contains the arduino code that enables sensors to send vital signs to raspberry gateway

# In src dir you will find the code

# Dependencies of project will be allowed in dependencies folder
